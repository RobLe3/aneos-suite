# aNEOS Core Requirements - Essential Dependencies Only
# This file contains only the essential dependencies needed for core NEO analysis

# Essential Scientific Computing
numpy>=1.24.0
astropy>=5.3.0
requests>=2.28.0
python-dateutil>=2.8.0

# NEO Data Sources (Optional - graceful fallback)
astroquery>=0.4.6  # For MPC and Horizons APIs

# Core Analysis
tqdm>=4.64.0  # Progress bars
humanize>=4.4.0  # Human-readable formatting

# Geospatial (for NEO distance calculations)
geopy>=2.3.0

# User Interface (Optional - graceful fallback)
rich>=13.0.0  # Rich terminal output

# Data Processing
scikit-learn>=1.1.0  # For anomaly detection algorithms

# Sky mechanics (Optional - graceful fallback)
skyfield>=1.46  # For astronomical calculations

# Additional utilities that enhance but don't break core functionality
matplotlib>=3.6.0  # For plotting (optional)
pandas>=1.5.0  # For data analysis (optional)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
hypothesis>=6.92.0

# Async HTTP (used in data/sources/base.py)
aiohttp>=3.9.0

# System monitoring (used in monitoring/metrics.py)
psutil>=5.9.0

# Validation modules (statistical_testing.py, spectral_outlier_analysis.py)
scipy>=1.10.0

# API / database layer (required by tests that import aneos_api)
pydantic>=2.5.0
sqlalchemy>=2.0.0
fastapi>=0.104.0
httpx>=0.25.0  # FastAPI/Starlette test client
python-jose[cryptography]>=3.3.0  # JWT auth (test_auth.py)