[pytest]
testpaths = tests aneos_core/tests
asyncio_mode = auto
markers =
    network: marks tests requiring live network (skip with -m 'not network')
    integration: marks integration tests that may be slower
